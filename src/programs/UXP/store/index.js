
import { navigation } from './navigation.module';
import { users } from './users.module';
import { employees } from './employees.module';

export const UXPStore = {
  modules: {
    navigation,
    users,
    employees
  }
};